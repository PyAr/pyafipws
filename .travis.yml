dist: xenial   # required for Python >= 3.7
language: python

python:
      - "3.5"
      - "3.6"
      - "3.7"
      - "3.8-dev"
install:
      - "python setup.py -q install"

env:
  - cert=$CERT
  - pkey=$PKEY

script:
  - pytest

branches:
  only:
    - travis

    